function Car(make, speed) {
  this.make = make;
  this.speed = speed;

  this.accelerate = function() {
    this.speed += 10;
    console.log(this.speed);
  };

  this.brake = function() {
    this.speed -= 5;
    console.log(this.speed);
